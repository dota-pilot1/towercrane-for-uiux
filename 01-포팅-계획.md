# 스터디 다이어리(Dev Study) 포팅 계획

> towercrane-for-uiux 의 "스터디 다이어리" 기능을 palantier(TechCannon)로 이식하는 계획서
> 작성일: 2026-07-24

---

## 1. 배경 · 목표

towercrane 의 **스터디 다이어리**는 다음 3열 구조로 학습 노트를 "위에서 아래로 순차 정리"하는 기능이다.

```
워크스페이스(study diary)
  └─ 1차 주제 (드래그 순서)      ← challenge_categories
       └─ 2차 주제 (드래그 순서)  ← challenge_sections
            └─ 노트 (pinned/최신순, 리치 블록)  ← challenge_user_notes
```

기존 palantier 의 **문서(Docu)** 는 자유형 폴더 트리 + 리치 에디터라 "순서(sequence)" 개념이 없다.
스터디 다이어리는 **모든 레벨에 명시적 순서(order_idx)** 가 있어 커리큘럼형 학습 정리에 적합 → 문서(Docu)와 역할이 달라 공존 정당함.

프로젝트 아키텍처 정리 용도로도 매핑이 자연스럽다:
- 1차 주제 = 도메인(`work`, `meeting`, `auth` …)
- 2차 주제 = 세부 모듈/개념(순서대로)
- 노트 = 실제 결정·트러블슈팅 정리

---

## 2. 스택 비교 (결론: "서버 재구현 · 프론트 재사용")

| 구분 | towercrane | palantier | 이식 방식 |
|------|-----------|-----------|----------|
| 백엔드 | NestJS + Drizzle + SQLite | **Spring Boot + MyBatis + PostgreSQL** | **재구현** (설계만 참고) |
| 프론트 | React 19 + Vite + TanStack Router, **fetch + zustand** | React 19 + Vite + TanStack Router, **axios + TanStack Query + shadcn** | **재사용** (auth/스타일만 교체) |
| DnD | @dnd-kit | @dnd-kit **이미 설치됨** | 그대로 |
| 에디터 | Lexical v0.43 (~15파일) | Lexical v0.42 **이미 사용중** (BlockEditor/BlockViewer) | **기존 것 재사용** |
| 이미지 업로드 | /upload/presign + S3 | `UploadController` + `S3Service` **이미 있음** | 기존 것 재사용 |
| 데스크톱 의존 | 없음(순수 웹) | — | 문제 없음 |

> ⚠️ towercrane 서버는 NestJS/SQLite 라 "서버 그대로 붙이기"는 **불가능**. 백엔드는 Java/MyBatis로 새로 작성한다.

---

## 3. 핵심 발견 — palantier에 이미 있는 것

가장 무거운 부분(Lexical 스택, dnd-kit, S3 업로드)을 **다시 포팅할 필요 없음.**

- **Lexical 에디터**: `lexical ^0.42.0` + 전체 `@lexical/*` 설치됨. `features/task/components/TaskBlockEditor.tsx`, `features/subutai-docu/components/SubutaiDocuBlockEditor.tsx` 등에서 **towercrane과 동일한 BlockEditor/BlockViewer 패턴을 이미 쓰고 있음** → 노트 에디터는 이걸 그대로 재사용.
- **@dnd-kit** (core/sortable/utilities) 설치됨 → 3열 드래그 정렬 그대로 구현 가능.
- **S3 업로드**: `upload/presentation/UploadController.java`, `upload/application/S3Service.java` 존재 → 이미지 업로드 재사용.
- **카테고리→섹션 지식베이스 패턴**: apidoc/architecture/core/devops/faq/frontend/security/skillcore/springai/textbook/subutai 등 10+ 도메인이 이미 `*_categories` / `*_sections` 구조를 반복 사용 → 컨벤션 확립돼 있음.
- **`challenge` 도메인**: `challenge_categories`, `challenge_sections`, `challenge_topics`, `challenge_submissions` 테이블 + 백엔드/프론트 존재.

### 이름 충돌 주의 (중요)
palantier 네임스페이스가 이미 붐빈다:
- `study_*` 테이블 = **posts/comments/likes 게시판형** (다이어리와 다른 기능)
- `challenge_*` 테이블 = 챌린지 기능 (submissions 기반, 노트 아님)

→ towercrane처럼 `challenge_*` 를 재활용하면 **기존 챌린지 기능과 충돌**한다. 새 도메인/테이블로 분리하는 것을 권장(§5).

---

## 4. 실제로 만들어야 하는 것 (진짜 갭)

| 레이어 | 신규 작업 | 재사용 |
|--------|----------|--------|
| DB | `study_diaries`(워크스페이스), 1차/2차 주제, 순서형 노트 테이블 4개 | — |
| 백엔드 | `studydiary` 도메인 (domain/infra/app/presentation) + 공개 컨트롤러 | S3, 인증필터, 공통 예외 |
| 프론트 타입/API | `entities/studyDiary` (types + axios api) | — |
| 프론트 훅 | `features/studyDiary/hooks` (TanStack Query + 낙관적 reorder) | — |
| 프론트 컴포넌트 | 3열(사이드바/섹션리스트/노트패널) + 노트 다이얼로그 | **BlockEditor/BlockViewer**, shadcn Dialog, dnd-kit |
| 페이지 | 워크스페이스 그리드 / 3열 상세 / 공개 뷰 | — |
| 라우팅 | `src/app/routes/index.tsx` 코드 등록 (`?cat=&sec=` 검색파라미터) | — |
| 메뉴 | `menus` 테이블 등록 (로컬+배포 DB 양쪽) | — |

---

## 5. 결정 필요 사항 (구현 착수 전)

1. **도메인 분리 방식** ★핵심
   - (A, 권장) **새 `studydiary` 도메인 + 새 테이블**(`study_diary_*`) — 기존 `challenge`/`study` 기능과 완전 분리, 충돌 없음.
   - (B) 기존 `challenge_*` 확장 — towercrane과 동일하나 palantier 챌린지 기능과 얽힘. 비권장.
2. **AI 자동정리** (`/notes/organize`, towercrane는 OpenAI gpt-4o-mini로 노트를 마크다운 정리)
   - (권장) v1에서 **제외**. 이후 palantier `springai` 도메인으로 연동 가능.
3. **노트 content 포맷**
   - palantier 기존 BlockEditor가 출력하는 포맷(블록 wrapper JSON / Lexical state)에 맞춘다 → 기존 뷰어와 호환.
4. **메뉴 위치·이름**: "Dev Study" 신규 최상위 메뉴 vs 기존 문서/지식 계열 하위. (경미, 나중 결정 가능)

---

## 6. 단계별 구현 계획

### Phase 1 — DB 스키마 (수동 SQL, 로컬+배포 양쪽)
`study_diaries` / `study_diary_categories`(1차) / `study_diary_sections`(2차) / `study_diary_notes`(노트).
- PK는 `varchar(36)` UUID(프론트가 string id 기대), `user_id`는 palantier 관례대로 `bigint`.
- 순서: `order_idx integer NOT NULL DEFAULT 0` (노트는 순서 없음 → `pinned DESC, updated_at DESC`).
- FK `ON DELETE CASCADE` (카테고리 삭제 → 섹션 → 노트 연쇄).
- DDL 초안은 §7.

### Phase 2 — 백엔드 (DDD, `com.mapo.palantier.studydiary`)
```
studydiary/
  domain/        StudyDiary, DiaryCategory, DiarySection, DiaryNote (+ 각 Repository 인터페이스)  @Getter @Builder
  infrastructure/ *Mapper(@Mapper), *RepositoryImpl
  application/    StudyDiaryService (Repository 인터페이스만 주입)
  presentation/   StudyDiaryController, StudyDiaryPublicController, dto/
```
- XML 매퍼는 `resources/mybatis/mapper/`, namespace = infrastructure 경로.
- 엔드포인트(towercrane 스펙 기준):
  - 워크스페이스: `GET/POST /study-diary/workspaces`, `GET/PATCH /study-diary/workspaces/{id}`
  - 1차: `GET/POST .../workspaces/{id}/categories`, `POST .../categories/reorder` `{categoryIds:[]}`, `PATCH/DELETE /study-diary/categories/{id}`
  - 2차: `GET .../categories/{cid}/sections`, `POST /study-diary/sections`, `POST /study-diary/sections/reorder` `{categoryId, sectionIds:[]}`, `PATCH/DELETE /study-diary/sections/{id}`
  - 노트: `GET .../sections/{sid}/notes/mine`, `POST /study-diary/notes`, `PATCH/DELETE /study-diary/notes/{id}`
  - 공개: `GET /study-diary/public/workspaces/{id}`(+categories/sections/notes) — `visibility=private`면 403, URL의 id가 곧 공유링크.
- 로직: 소유자 검증(모든 mutation), reorder=id 배열 받아 0..n 재인덱싱, 다음 순서=`MAX(order_idx)+1`.
- 인증: towercrane SessionGuard → palantier JWT 필터의 현재 사용자 id 사용.

### Phase 3 — 프론트 (FSD)
```
entities/studyDiary/
  types/studyDiary.ts            (Workspace/Category/Section/Note 인터페이스)
  api/studyDiaryApi.ts           (axios, baseURL에 이미 /api → 경로는 /study-diary/…)
features/studyDiary/
  hooks/useStudyDiary.ts         (TanStack Query, 낙관적 reorder onMutate/onError/onSettled)
  components/
    StudyDiarySidebar.tsx        (1차 주제 열, dnd-kit SortableContext)
    StudyDiarySectionList.tsx    (2차 주제 열, dnd-kit)
    StudyDiaryNotesPanel.tsx     (노트 열 — 기존 BlockEditor/BlockViewer 재사용)
    NoteFormDialog.tsx           (shadcn Dialog)
pages/studyDiary/
  StudyDiaryHomePage.tsx         (워크스페이스 그리드)
  StudyDiaryPage.tsx             (3열 상세, useSearch로 ?cat=&sec= 선택상태 URL 보존)
  StudyDiaryPublicPage.tsx       (읽기전용)
```
- towercrane 컴포넌트에서 **로직(dnd 배선, reorder 낙관적 업데이트, 컬럼 구조)**은 그대로 가져오되:
  - fetch+zustand → palantier axios 인스턴스 + 기존 auth 훅
  - towercrane 커스텀 디자인토큰(`ui-text-*`, `bg-brand-glass`…) → palantier shadcn/Tailwind 토큰으로 교체(기존 컴포넌트 위에 재작성하므로 자연 해결)
  - 직접 Radix Dialog → palantier shadcn `Dialog`
  - 노트 에디터는 신규 포팅 대신 **기존 BlockEditor/BlockViewer** 사용.

### Phase 4 — 라우팅 · 메뉴
- `src/app/routes/index.tsx`에 3개 라우트 코드 등록(파일라우팅 금지 규칙 준수), `?cat=&sec=` `validateSearch`.
- `menus` 테이블에 로컬+배포 DB 양쪽 등록.
- `npx tsc --noEmit` + 백엔드 `compileJava` 통과 확인.

### Phase 5 (선택) — AI 자동정리
palantier `springai` 도메인 연동해 노트 마크다운 정리 재현.

---

## 7. DDL 초안 (PostgreSQL, 옵션 A 기준)

```sql
CREATE TABLE study_diaries (
  id          varchar(36) PRIMARY KEY,
  user_id     bigint      NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  title       varchar(255) NOT NULL,
  description varchar(500),
  visibility  varchar(20) NOT NULL DEFAULT 'private',   -- private|shared|public
  created_at  timestamptz NOT NULL DEFAULT now(),
  updated_at  timestamptz NOT NULL DEFAULT now()
);

CREATE TABLE study_diary_categories (              -- 1차 주제
  id         varchar(36) PRIMARY KEY,
  diary_id   varchar(36) NOT NULL REFERENCES study_diaries(id) ON DELETE CASCADE,
  name       varchar(255) NOT NULL,
  summary    varchar(500),
  icon       varchar(50) NOT NULL DEFAULT 'Trophy',
  order_idx  integer     NOT NULL DEFAULT 0,
  created_by bigint      NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  created_at timestamptz NOT NULL DEFAULT now(),
  updated_at timestamptz NOT NULL DEFAULT now()
);

CREATE TABLE study_diary_sections (                -- 2차 주제
  id          varchar(36) PRIMARY KEY,
  category_id varchar(36) NOT NULL REFERENCES study_diary_categories(id) ON DELETE CASCADE,
  title       varchar(255) NOT NULL,
  summary     varchar(500),
  order_idx   integer     NOT NULL DEFAULT 0,
  created_at  timestamptz NOT NULL DEFAULT now(),
  updated_at  timestamptz NOT NULL DEFAULT now()
);

CREATE TABLE study_diary_notes (                   -- 노트
  id         varchar(36) PRIMARY KEY,
  user_id    bigint      NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  section_id varchar(36) REFERENCES study_diary_sections(id) ON DELETE CASCADE,
  title      varchar(255),
  content    text        NOT NULL,                 -- 블록 wrapper JSON(Lexical state)
  visibility varchar(20) NOT NULL DEFAULT 'private',
  pinned     boolean     NOT NULL DEFAULT false,
  created_at timestamptz NOT NULL DEFAULT now(),
  updated_at timestamptz NOT NULL DEFAULT now()
);

CREATE INDEX idx_sddc_diary    ON study_diary_categories(diary_id);
CREATE INDEX idx_sdds_category ON study_diary_sections(category_id);
CREATE INDEX idx_sdn_section   ON study_diary_notes(section_id);
```

---

## 8. 재사용 매핑 요약

| towercrane 요소 | palantier 처리 |
|-----------------|----------------|
| Lexical 15파일 에디터 | 기존 `TaskBlockEditor`/`SubutaiDocuBlockEditor` 재사용 |
| @dnd-kit 정렬 배선 | 설치됨 → 로직 그대로 |
| fetch + zustand 토큰 | axios 인스턴스 + 기존 auth |
| /upload/presign + S3 | `UploadController` 재사용 |
| Drizzle 스키마 | 새 PostgreSQL 테이블(§7) |
| NestJS 컨트롤러/서비스 | Java/MyBatis 재구현 |
| 커스텀 디자인 토큰 | shadcn/Tailwind로 재작성 |
| SessionGuard | JWT 인증 필터 |

---

## 9. 리스크 · 주의점

- **네임스페이스 충돌**: `study_*`(게시판), `challenge_*`(챌린지)가 이미 점유 → 새 `study_diary_*` 로 분리(옵션 A).
- **노트 content 포맷 호환**: 반드시 palantier 기존 BlockEditor 출력 포맷에 맞출 것(다르면 뷰어 깨짐).
- **메뉴 parent_id 로컬/배포 상이**(CLAUDE.md 명시) → 등록 시 양쪽 값 다름 주의.
- **pre-push hook**: push 전 `tsc -b && vite build` 자동 실행 → 미사용 import 정리 필수.
- **공유 URL = id 노출**: 공개 워크스페이스는 URL의 id가 곧 접근키(토큰 없음). 민감 정보 노트에 주의.

---

## 10. 다음 액션

1. §5 결정(특히 옵션 A/B, AI 포함 여부) 확정
2. Phase 1 DDL 로컬 DB 적용 → Phase 2 백엔드 → Phase 3 프론트 순차 진행
3. 각 Phase 후 빌드 체크(`tsc --noEmit`, `compileJava`)
